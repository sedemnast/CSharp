using static System.Console;

Clear(); // Метод 1, который ничего не принимает и ничего не возвращает.
WriteLine("Привет!"); // Метод 2, который что-то принимает, но ничего не возвращает.
string num = ReadLine(); //Метод 3, который ничего не принимает, но что-то возвращает.
int number = int.Parse(num); //Метод 4, который и принимает, и возвращает.